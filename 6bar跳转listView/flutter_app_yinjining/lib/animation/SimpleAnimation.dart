import 'package:flutter/material.dart';

// 资讯列表页面
class SimpleAnimation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("SimpleAnimation"),
    );
  }
}